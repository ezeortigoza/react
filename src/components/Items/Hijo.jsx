/*  import React, { useContext } from 'react'
import { miContexto } from '../../App'


function Hijo() {
    const {darkMode,setDarkMode} = useContext(miContexto);
  return (
    <div style={{backgroundColor: darkMode ? 'white' : 'black', color: darkMode ? 'dark' : 'white' }}>
        Hijo del Home 
        <button className='btn btn-outline-dark' onClick={()=>setDarkMode(!darkMode)}>Cambiar de color</button>
    </div>
  )
}

export default Hijo  */